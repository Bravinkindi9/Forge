# Vercel Postgres (set up in Milestone 1)
POSTGRES_URL=

# Gemini API (needed starting Milestone 4)
GEMINI_API_KEY=

# Jina Reader — no key required for the free r.jina.ai endpoint used in Milestone 3.
# Only add this if we hit rate limits and need a higher-tier key.
JINA_API_KEY=
